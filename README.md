# stackoverflow-tag-parser
To run the program:
```
./gradlew run -Pquery="<query string>"
```
Where `<query string>` is a query to be submitted to SO search engine. Tags from the first 50 results will be analyzed and top 5 tags will be printed
